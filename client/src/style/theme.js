import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#D9782D', // CSU orange color
    },
    secondary: {
      main: '#1E4D2B', // Optional secondary color
    },
  },
});

export default theme;
